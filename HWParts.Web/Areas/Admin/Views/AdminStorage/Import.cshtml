@model HWParts.Core.Application.ViewModels.Storage.ImportStoragesViewModel
@{
    ViewData["Title"] = "Importar Processadores";
}

@using (@Html.BeginForm(
    "Import",
    "AdminStorage",
    FormMethod.Post,
    new { enctype = "multipart/form-data" }))
{
    @Html.TextBoxFor(x => x.File, new { type = "file" })

    <button type="submit" class="btn btn-block btn-primary">
        Importar
    </button>
}